name: neural-from-scratch

services:
  visualizations:
    image: ${IMAGE_REF:-neural-from-scratch-visualizations:latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${VIS_PORT:-8080}:80"
    restart: unless-stopped
